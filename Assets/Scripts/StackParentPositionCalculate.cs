using System.Collections;
using System.Collections.Generic;
using UnityEngine;
[ExecuteAlways]
public class StackParentPositionCalculate : MonoBehaviour
{
   [SerializeField]
   private StackManager stackManager;

   /*void Awake() => transform.position = new Vector3(stackManager.body.transform.position.x,
        stackManager.body.transform.position.y + stackManager.stackableObjectTransform.localScale.y/
       - stackManager.stackableObjectTransform.GetChild(0).localScale.y * 2 *
        stackManager.stackableObjectTransform.localScale.y,
        stackManager.body.transform.position.z);*/
   
}
